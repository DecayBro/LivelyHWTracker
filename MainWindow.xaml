<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="LivelyHWTracker.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LivelyHWTracker"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="LivelyHWTracker"
    Closed="Window_Closed"
    >
    <StackPanel Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Top">
        <StackPanel.Resources>
            <Style TargetType="TextBlock" x:Key="CustomTextBlockStyle">
                <Setter Property="FontFamily" Value="Comic Sans MS"/>
                <Setter Property="FontStyle" Value="Italic"/>
            </Style>
        </StackPanel.Resources>
        <TextBlock Text="{x:Bind ViewModel.CpuTemperature}" Style="{StaticResource CustomTextBlockStyle}"/>
        <TextBlock Text="GPU TEMP: XXX" Style="{StaticResource CustomTextBlockStyle}"/>
    </StackPanel>
</Window>
